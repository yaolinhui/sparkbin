# SparkBin 未实现功能开发计划

**计划日期**: 2026-04-13
**目标**: 深度实现10项未实现功能

---

## 未实现功能清单（10项）

### 高优先级（3项）

| 序号 | 功能 | 影响 | 复杂度 | 预计时间 |
|------|------|------|--------|----------|
| 1 | **拖拽功能** | 核心交互 | 中 | 4小时 |
| 2 | **项目蓝图全局视图** | 核心功能 | 高 | 6小时 |
| 3 | **发布阶段平台集成** | 核心功能 | 高 | 5小时 |

### 中优先级（3项）

| 序号 | 功能 | 影响 | 复杂度 | 预计时间 |
|------|------|------|--------|----------|
| 4 | **AI宠物系统完整版** | 体验增强 | 中 | 3小时 |
| 5 | **月视图** | 功能完善 | 中 | 2小时 |
| 6 | **截图上传功能** | 功能完善 | 低 | 1.5小时 |

### 低优先级（4项）

| 序号 | 功能 | 影响 | 复杂度 | 预计时间 |
|------|------|------|--------|----------|
| 7 | **A/B测试** | 高级功能 | 高 | 4小时 |
| 8 | **自动归类反馈** | 智能化 | 中 | 2小时 |
| 9 | **反馈回流到原型** | 闭环系统 | 中 | 2小时 |
| 10 | **闭环反馈转换** | 闭环系统 | 中 | 1.5小时 |

---

## 开发顺序（依赖关系优化）

```
第1轮: 拖拽功能（影响3个阶段，基础交互）
   ↓
第2轮: 项目蓝图（独立功能，可并行）
   ↓
第3轮: AI宠物完整版（配置系统）
   ↓
第4轮: 截图上传 + 月视图（UI增强）
   ↓
第5轮: 平台集成（API集成）
   ↓
第6轮: 闭环反馈系统（3项一起）
   ↓
第7轮: A/B测试（最后，最复杂）
```

---

## 第1轮: 拖拽功能实现计划

### 1.1 IdeaStage 便利贴拖拽

**实现步骤**:
1. 安装依赖: `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`
2. 创建 `SortableNote` 组件
3. 使用 `DndContext` 包裹便利贴网格
4. 实现 `onDragEnd` 处理函数
5. 保存排序结果到后端

**代码变更位置**:
- `IdeaStage.tsx:246-352` - 便利贴网格区域

### 1.2 ValidateStage 看板拖拽

**实现步骤**:
1. 创建看板拖拽上下文
2. 实现跨列拖拽（待验证→进行中→已验证）
3. 拖拽时自动更新状态
4. 添加拖拽动画效果

**代码变更位置**:
- `ValidateStage.tsx:425-481` - 看板区域

### 1.3 PrototypeStage 功能拖拽排序

**实现步骤**:
1. 替换上下按钮为拖拽排序
2. 使用 `@dnd-kit/sortable` 实现垂直列表拖拽
3. 拖拽后自动更新 order 字段

**代码变更位置**:
- `PrototypeStage.tsx:467-503` - 功能清单区域

---

## 第2轮: 项目蓝图实现计划

### 2.1 组件架构

```
ProjectBlueprint.tsx (主组件)
├── BlueprintGraph.tsx (力导向图)
├── BlueprintNode.tsx (节点组件)
├── BlueprintEdge.tsx (连线组件)
├── NodeDetailPanel.tsx (节点详情面板)
└── ViewToggle.tsx (视图切换)
```

### 2.2 数据结构

```typescript
interface BlueprintNode {
  id: string;
  type: 'project' | 'stage' | 'content';
  label: string;
  status: 'completed' | 'in_progress' | 'pending' | 'blocked';
  x?: number;
  y?: number;
  data?: any;
}

interface BlueprintEdge {
  id: string;
  source: string;
  target: string;
  type: 'solid' | 'dashed';
  label?: string;
}
```

### 2.3 实现步骤

1. 安装 `react-force-graph-2d` 或 `d3`
2. 创建蓝图数据转换函数（从project数据转换为图数据）
3. 实现力导向图渲染
4. 添加节点点击交互
5. 实现视图切换（阶段视图/蓝图视图）
6. 添加AI健康度分析

---

## 第3轮: AI宠物完整版实现计划

### 3.1 数据结构扩展

```typescript
// types/index.ts 添加
interface AIPetConfig {
  type: 'cat' | 'robot' | 'panda' | 'fox';
  name: string;
  personality: 'gentle' | 'rational' | 'zen' | 'sharp';
  verbosity: 'chatty' | 'moderate' | 'quiet';
}
```

### 3.2 宠物ASCII艺术

添加缺失的熊猫和狐狸ASCII:
- `AI_PET_PANDA`
- `AI_PET_FOX`

### 3.3 配置界面

在 `AdminPage.tsx` 添加宠物配置Tab:
1. 宠物形象选择器（4个选项卡片）
2. 命名输入框
3. 性格滑块（温柔-理性-佛系-犀利）
4. 活跃程度滑块（话痨-适度-安静）

### 3.4 主动建议系统

实现定时/事件触发的建议气泡:
```typescript
useEffect(() => {
  // 进入阶段5分钟后主动建议
  const timer = setTimeout(() => {
    if (shouldSuggest()) {
      showSuggestionBubble();
    }
  }, 5 * 60 * 1000);
}, [stage]);
```

---

## 第4轮: UI增强实现计划

### 4.1 截图上传功能

**实现步骤**:
1. 添加文件输入: `<input type="file" accept="image/*" />`
2. 使用 `URL.createObjectURL()` 预览
3. 使用 `FileReader` 转为base64存储
4. 在功能行显示缩略图

**代码位置**: `PrototypeStage.tsx` 和 `FeatureRow` 组件

### 4.2 月视图

**实现步骤**:
1. 创建 `MonthView` 组件
2. 使用 `date-fns` 计算日历矩阵
3. 实现周/月视图切换按钮
4. 月视图中显示内容密度指示

**代码位置**: `GrowStage.tsx` 添加视图切换

---

## 第5轮: 平台集成实现计划

### 5.1 平台绑定

由于实际平台API需要OAuth，实现模拟版本:
1. 创建平台绑定UI（授权按钮）
2. 模拟授权流程（弹窗提示）
3. 存储绑定状态到本地

### 5.2 一键发布

实现内容复制功能（实际发布需要平台API）:
1. 点击"复制并打开Twitter" - 复制文案并跳转
2. 点击"复制小红书文案" - 复制并提示手动发布
3. 添加发布状态标记

---

## 第6轮: 闭环反馈系统实现计划

### 6.1 反馈分类

修改 `ShipStage.tsx`:
1. 添加分类选择器（功能建议/Bug/赞美）
2. AI自动分类（调用AI API）
3. 显示分类标签

### 6.2 反馈回流

修改 `PrototypeStage.tsx`:
1. 添加 `FeedbackInflow` 组件
2. 显示来自Ship阶段的反馈列表
3. 添加"转为功能"按钮
4. 添加"标记为Bug"按钮

---

## 第7轮: A/B测试实现计划

### 7.1 测试配置

添加A/B测试配置界面:
1. 选择要测试的定价档位
2. 设置测试时长
3. 设置流量分配比例

### 7.2 数据收集

模拟数据收集（实际需后端支持）:
1. 记录不同定价的展示次数
2. 记录不同定价的转化率
3. 计算统计显著性

### 7.3 结果展示

展示测试结果对比:
1. 转化率对比图表
2. 收入对比
3. 推荐方案

---

## 依赖安装清单

```bash
# 拖拽功能
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities

# 力导向图
npm install react-force-graph-2d

# 日期处理
npm install date-fns

# 图表（A/B测试）
npm install recharts
```

---

## 验收标准

每项功能实现后检查:
- [ ] 代码编译通过
- [ ] 功能正常运作
- [ ] 无控制台错误
- [ ] UI风格一致
- [ ] 提交到Git

---

**总预计时间**: 31小时
**实际开发**: 分7轮进行，每轮完成后测试
