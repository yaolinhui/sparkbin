# 架构决策记录（DECISIONS）

> **文档用途**：记录 SparkBin 项目中所有关键架构决策，防止 AI 在新会话中"忘记"已确定的方案，避免反复讨论同一问题。
>
> **维护规则**：每次做出新的架构决策时，追加到本文档末尾。决策一旦记录，除非经过充分讨论，否则不随意推翻。
>
> **更新时间**：2026-04-24 16:06:47

---

## 决策列表

### DEC-001：前后端分离架构

**日期**：2026-04-08

**决策内容**：将原本纯前端（localStorage）的 SparkBin 改造为前后端分离架构。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| 保持纯前端 + localStorage | 简单，无部署成本 | 数据易丢失，无法多设备同步，AI 配置不安全 |
| 纯前端 + GitHub JSON 备份 | 有云端备份 | 需要暴露 GitHub Token，操作复杂 |
| **前后端分离（选中）** | 数据安全持久化，AI Key 存后端，可扩展用户系统 | 需要维护后端服务，部署成本增加 |

**决策理由**：
- AI 配置（API Key）必须安全存储，不能放在前端
- 需要用户认证和权限管理（admin / user）
- 未来可能需要多设备同步和协作功能

**影响范围**：全部项目架构

---

### DEC-002：后端技术栈选 FastAPI + SQLAlchemy

**日期**：2026-04-08

**决策内容**：后端使用 Python FastAPI 框架，ORM 使用 SQLAlchemy 2.0，数据库支持 SQLite（开发）和 PostgreSQL（生产）。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| Node.js + Express + Prisma | 前后端同语言 | 对 AI 生成代码的友好度不如 Python |
| **FastAPI + SQLAlchemy（选中）** | 现代、类型安全、自动 API 文档、AI 生成质量高 | 需要维护 Python 环境 |
| Django | 功能全、生态成熟 | 过重，不适合轻量级 API 服务 |

**决策理由**：
- FastAPI 的 Pydantic 模型与前端 TypeScript 类型天然对应
- SQLAlchemy 2.0 支持类型提示，AI 不容易写错
- SQLite 适合本地开发，零配置启动

**影响范围**：`backend/` 目录全部代码

---

### DEC-003：AI 代理模式（后端代理）

**日期**：2026-04-09

**决策内容**：前端不直接调用任何 AI 提供商的 API，所有 AI 请求通过后端代理转发。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| 前端直接调用 OpenAI/DeepSeek API | 简单，减少后端负载 | API Key 暴露在前端，安全风险极高 |
| **后端代理模式（选中）** | API Key 安全存储（加密），可统一限流/日志/切换模型 | 增加后端复杂度，需要维护多提供商适配 |

**决策理由**：
- API Key 安全性是底线，绝对不能暴露到前端
- 后端可以统一记录 AI 调用日志（AICallLog）
- 可以在后端实现模型切换、失败重试等逻辑

**影响范围**：`backend/app/services/ai_proxy.py`，`frontend/src/services/ai.ts`

---

### DEC-004：Brutalist（粗野主义）设计风格

**日期**：2026-04-07

**决策内容**：采用 Brutalist 设计风格——等宽字体（JetBrains Mono）、零圆角边框、高对比度、深色/浅色双主题。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| 现代圆润 Material Design | 大众接受度高 | 千篇一律，缺乏个性 |
| 纸张纹理拟物风 | 有质感 | 实现复杂，与深色主题冲突 |
| **Brutalist 粗野主义（选中）** | 独特、高识别度、与"开发者工具"定位契合、实现简单 | 部分用户可能觉得"太硬" |

**决策理由**：
- 目标用户是 Vibe 开发者 / Indie Hacker，喜欢有个性的工具
- Brutalist 风格与终端/代码编辑器的视觉语言一致
- 零圆角 + 等宽字体在 Tailwind 中极易实现

**影响范围**：`frontend/src/index.css`，`frontend/tailwind.config.js`

---

### DEC-005：SSE 流式响应（而非 WebSocket）

**日期**：2026-04-10

**决策内容**：AI 聊天使用 SSE（Server-Sent Events）实现流式响应，而非 WebSocket。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| WebSocket | 双向实时，适合高频交互 | 需要维护连接状态，前后端复杂度增加，防火墙/Nginx 配置麻烦 |
| **SSE（选中）** | 基于 HTTP，简单，自动重连，前端用 EventSource 即可 | 仅服务端向客户端推送，不适合双向高频场景 |

**决策理由**：
- AI 聊天是"一问一答"模式，SSE 完全够用
- SSE 基于标准 HTTP，调试简单（curl 可直接测试）
- 不需要维护 WebSocket 连接状态，减少 bug 来源

**影响范围**：`backend/app/routers/ai.py`（POST /chat），`frontend/src/services/ai.ts`

---

### DEC-006：六阶段 Vibe 工作流（IDEA → MONETIZE）

**日期**：2026-04-07

**决策内容**：项目生命周期固定为六个阶段：想法(IDEA) → 验证(VALIDATE) → 原型(PROTOTYPE) → 发布(SHIP) → 增长(GROW) → 变现(MONETIZE)。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| 自由看板（任意列） | 灵活 | 缺乏引导，新手不知道该怎么走 |
| **固定六阶段（选中）** | 提供清晰的 Indie Hacker 路径，每个阶段有明确目标和退出标准 | 不够灵活，某些项目可能跳过某些阶段 |

**决策理由**：
- 产品定位就是"帮助 Vibe 开发者系统地推进项目"
- 固定阶段提供仪式感，降低决策疲劳
- 每个阶段可以设计专用的编辑器和 AI 辅助逻辑

**影响范围**：`frontend/src/types/index.ts`（StageKey），`backend/app/models.py`，六阶段组件

---

### DEC-007：命名导出（禁用默认导出）

**日期**：2026-04-07

**决策内容**：项目中全部使用命名导出（`export function Xxx`），禁止使用默认导出（`export default`）。

**决策理由**：
- 命名导出在重构时更容易搜索和替换
- IDE 自动补全更精确
- 避免默认导出导致的重命名混乱

**影响范围**：全部前端 `src/` 代码

---

### DEC-008：interface 优先于 type

**日期**：2026-04-07

**决策内容**：TypeScript 类型定义优先使用 `interface`，而非 `type`。

**决策理由**：
- interface 支持声明合并，扩展性更好
- interface 错误提示更友好
- 与面向对象风格更一致

**影响范围**：`frontend/src/types/index.ts` 及所有类型定义

---

### DEC-009：前端状态管理使用 Zustand（而非 Redux）

**日期**：2026-04-08

**决策内容**：前端状态管理使用 Zustand，不使用 Redux 或 Context + useReducer。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| Redux + RTK | 生态成熟，调试工具强 | 样板代码多，对 AI 不友好 |
| Context + useReducer | 无需额外依赖 | 性能差，组件树深时 re-render 严重 |
| **Zustand（选中）** | API 极简，无样板代码，TypeScript 友好，AI 容易写对 | 生态不如 Redux 丰富 |

**决策理由**：
- Zustand 的 API 极简，AI 生成的代码不容易出错
- 不需要 Provider 包裹，减少嵌套
- 中间件支持持久化、日志等

**影响范围**：`frontend/src/stores/projectStore.ts`，`frontend/src/stores/aiStore.ts`

---

### DEC-010：JWT Token 存储于 localStorage

**日期**：2026-04-08

**决策内容**：用户认证使用 JWT，Token 存储在浏览器 localStorage 中。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| Cookie + HttpOnly | XSS 安全 | 需要处理 CSRF，跨域麻烦 |
| **localStorage + Bearer Token（选中）** | 实现简单，前后端分离友好 | XSS 攻击可窃取 Token |

**决策理由**：
- 简化实现，降低前后端复杂度
- 当前是单用户/小团队工具，安全要求不是最高优先级
- 如未来需要更高安全性，可迁移到 Cookie + Refresh Token 方案

**影响范围**：`frontend/src/services/api.ts`，`backend/app/auth.py`

---

### DEC-011：阶段原生格式（AI 回复固定四段结构）

**日期**：2026-04-12

**决策内容**：AI 回复项目相关内容时，必须包含固定四段结构：`【阶段事实】`、`【关键缺口】`、`【下一步动作】`、`【可同步JSON】`。

**决策理由**：
- 统一 AI 输出格式，便于前端解析和展示
- 可自动提取 `sync_payload` 同步到编辑器
- 可自动提取 `next_question` 作为下一轮提问建议
- 验证失败时自动重试，保证输出质量

**影响范围**：`backend/app/services/stage_context.py`，`backend/app/routers/ai.py`

---

### DEC-012：Stripe 测试模式集成

**日期**：2026-04-13

**决策内容**：支付功能仅集成 Stripe 测试模式，不接入真实支付。

**决策理由**：
- 当前阶段是 MVP 验证，真实支付不是最高优先级
- 测试模式可以免费验证支付流程的完整性
- 避免真实交易带来的法律和税务复杂性

**影响范围**：`backend/app/routers/payments.py`，`frontend/src/components/MonetizeStage.tsx`

---

### DEC-013：数据库迁移使用 Alembic，但禁止修改已有迁移文件

**日期**：2026-04-08

**决策内容**：数据库迁移使用 Alembic，但已创建的迁移文件禁止修改。如有 schema 变更，新增迁移文件。

**决策理由**：
- 修改已有迁移会导致团队环境不一致
- SQLite 开发环境对 ALTER TABLE 支持有限，新增迁移更安全
- 启动时动态添加缺失列作为兼容性补丁（`backend/app/main.py` 中的 `init_database`）

**影响范围**：`backend/alembic/versions/`

---

### DEC-014：前端路由使用 BrowserRouter（而非 HashRouter）

**日期**：2026-04-10

**决策内容**：前端路由使用 `BrowserRouter`，配合 Vite 的 `historyApiFallback`。

**决策理由**：
- URL 更美观（`/project/123` 而非 `/#/project/123`）
- 利于 SEO（虽然本项目不需要）
- Vite dev server 和生产构建都支持 history fallback

**影响范围**：`frontend/src/App.tsx`，`frontend/vite.config.ts`

---

### DEC-015：前端构建产物分块策略（manualChunks）

**日期**：2026-04-12

**决策内容**：Vite 构建使用 `rollupOptions.output.manualChunks` 手动分块，将大型依赖拆分为独立 chunk。

**决策理由**：
- react-force-graph、reactflow、@tiptap、@dnd-kit 等库体积大，独立加载减少首屏时间
- 缓存策略更优：第三方库变化少，可长期缓存

**影响范围**：`frontend/vite.config.ts`

---

### DEC-016：多用户认证体系（邮箱注册 + OAuth 2.0）

**日期**：2026-04-27

**决策内容**：将原有单一管理员认证系统升级为完整的多用户认证体系，支持邮箱注册（带邮件验证）和第三方 OAuth 登录（Google / GitHub）。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| 保持单管理员模式 | 实现最简单 | 无法支持多用户，无法商业化 |
| 仅邮箱注册 | 完全自主可控 | 用户注册门槛高，转化率低 |
| **邮箱注册 + OAuth 2.0（选中）** | 兼顾自主注册和快捷登录，国际化友好 | 需要维护 OAuth 应用配置，依赖第三方服务可用性 |

**决策理由**：
- 产品定位面向全球 Indie Hacker，必须支持国际主流登录方式
- OAuth（Google/GitHub）大幅降低注册门槛，提升转化率
- 邮箱注册作为兜底方案，覆盖无法使用 OAuth 的用户
- 邮件验证机制防止垃圾注册
- Resend 邮件服务在开发环境可 mock，零成本启动

**技术细节**：
- 授权码流程（Authorization Code Flow）：安全，Token 不暴露给前端
- 回调后通过 URL query param 传递 token 到前端，前端 `OAuthHandler` 自动处理登录
- JWT 双 Token 体系保持不变（access 15min + refresh 7d）
- User 模型扩展 `oauth_provider` / `oauth_id` 字段区分来源
- `username` / `password_hash` 改为 nullable 以兼容 OAuth 用户

**影响范围**：`backend/app/routers/auth.py`, `backend/app/models.py`, `backend/app/email.py`, `frontend/src/components/LoginModal.tsx`, `frontend/src/App.tsx`, `frontend/src/pages/VerifyEmailPage.tsx`, `frontend/src/pages/ResetPasswordPage.tsx`

---

### DEC-017：SQLite 数据库文件排除版本控制

**日期**：2026-04-27

**决策内容**：将 `*.db` / `*.sqlite` / `*.sqlite3` 加入 `.gitignore`，并从 git 索引中移除已跟踪的 `backend/sparkbin_v2.db`。

**决策理由**：
- SQLite 数据库文件包含运行时生成的用户数据、会话数据，属于环境状态而非源代码
- 提交数据库文件会导致仓库体积膨胀（每次提交都包含完整的二进制 diff）
- 不同开发者的本地数据库内容不一致，合并时会产生冲突
- 生产环境使用 PostgreSQL，SQLite 仅为开发用途

**影响范围**：`.gitignore`, `backend/sparkbin_v2.db`（停止跟踪，本地文件保留）

---

### DEC-018：产品方向转型——开源 + 社区会员模式

**日期**：2026-04-27

**决策内容**：将产品定位从纯商业 SaaS 转向**开源核心 + 社区会员增值**模式。核心功能完全开源免费，高级功能通过社区会员解锁。

**选项对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| 纯商业 SaaS（闭源） | 收入可控，代码保护 | 用户门槛高，社区无法参与，信任成本高 |
| 纯开源（无商业化） | 社区活跃，传播快 | 无持续收入，维护动力不足 |
| **开源核心 + 社区会员（选中）** | 社区驱动增长，核心功能透明可信，会员提供持续收入 | 需要维护两套功能边界，开源治理成本高 |

**决策理由**：
- 目标用户是 Indie Hacker，偏好可控、可审计、可自托管的工具
- 开源降低试用门槛，代码公开建立信任
- 社区会员（Pro/Team）为需要托管服务的用户提供便利，同时为项目提供可持续维护资金
- 自托管用户无限制（自带 API Key），与云服务形成差异化

**影响范围**：`README.md`（功能矩阵说明）、`LICENSE`（MIT）、`SELF_HOSTING.md`（部署指南）、功能门控系统（`backend/app/routers/` 中的权限检查）

---

### DEC-019：文档治理规则

**日期**：2026-04-27

**决策内容**：建立文档更新触发条件、提交规范（`docs:` 标签）、发版前文档冻结检查清单，防止文档与代码脱节。

**决策理由**：
- 项目快速迭代中，文档滞后是最常见的技术债务
- 需要可执行的规则而非依赖"有空再写"
- 开源项目文档是社区基础设施，文档质量直接等于项目专业度
- PR 模板中的"文档已更新"勾选项 + `docs:` 提交标签，形成最低成本的约束机制

**核心规则**：
1. 8 类必须更新文档的触发条件（新增 env 变量、API 端点、数据库字段、用户可见功能等）
2. 5 类不需要更新文档的场景（纯 UI 样式、内部重构等）
3. 发版前 5 项文档冻结检查清单
4. 文档债务跟踪表，定期 review

**影响范围**：`docs/02-文档更新规则.md`、`.github/PULL_REQUEST_TEMPLATE.md`、`CONTRIBUTING.md`

---

### DEC-020：全面安全加固（Security Hardening 2026-04-27）

**日期**：2026-04-27

**决策内容**：对 SparkBin 进行端到端安全审计并实施分层加固，覆盖认证、授权、输入验证、加密、日志脱敏、前端安全及传输安全。

**修复清单（按优先级）**：

| 优先级 | 领域 | 措施 | 文件 |
|--------|------|------|------|
| P0 | 启动安全 | 默认 SECRET_KEY / ENCRYPTION_KEY / admin/admin 强制拒绝启动，防止部署即裸奔 | `backend/app/config.py`, `backend/app/auth.py` |
| P0 | 数据隔离 | `/ai/call-logs` 增加 `user_id` 过滤，防止用户 A 查看用户 B 的调用记录 | `backend/app/routers/ai.py` |
| P0 | 信息泄露 | `/auth/me` 移除 `stripe_customer_id` / `stripe_subscription_id`，防止支付 ID 泄露 | `backend/app/routers/auth.py` |
| P0 | 信息泄露 | SSE 错误流仅返回 `"AI 服务暂时不可用，请稍后重试"`，隐藏内部异常类型和堆栈 | `backend/app/routers/ai.py` |
| P1 | XSS 防护 | AIChat 改用 `SafeMarkdown` 组件（`rehype-sanitize` + `skipHtml` + 禁用危险标签），替代裸 `ReactMarkdown` | `frontend/src/components/SafeMarkdown.tsx`, `AIChat.tsx` |
| P1 | 加密升级 | API Key 加密 salt 从硬编码改为 `SHA256(key)` 派生；PBKDF2 迭代从 100,000 提升至 600,000 | `backend/app/encryption.py` |
| P1 | 传输/Host 安全 | CORS `allow_methods` / `allow_headers` 从 `"*"` 收紧为显式白名单；启用 `TrustedHostMiddleware` | `backend/app/main.py`, `backend/app/config.py` |
| P1 | 代码卫生 | 移除所有 `print(..., file=sys.stderr)` 调试语句，统一使用 `logging` | `backend/app/services/ai_proxy.py` 等 |
| P2 | 密码策略 | 复杂度校验增加特殊字符要求（`!@#$%^&*...`）；`hash_password` 先 SHA-256 预哈希再 bcrypt，规避 72 字节截断 | `backend/app/auth.py` |
| P2 | 请求限制 | 新增 `RequestSizeLimitMiddleware` + `h11_max_request_size=10MB`，防止超大请求 DoS | `backend/app/main.py` |
| P2 | 日志脱敏 | admin 操作日志返回时递归红码敏感字段（`password`, `api_key`, `secret`, `token` 等） | `backend/app/routers/admin.py` |
| P2 | 前端安全 | 移除 `atob()` 客户端 JWT 解析，用户角色/ID 统一由 `/auth/me` 接口返回并缓存 | `frontend/src/services/api.ts`, `frontend/src/App.tsx` |
| P2 | 支付安全 | Stripe Webhook 使用 `stripe.Webhook.construct_event()` 进行签名验证，拒绝伪造回调 | `backend/app/routers/payments.py` |

**待完成的架构级改进（P3）**：
- [ ] Token 存储从 `localStorage` 迁移到 `httpOnly` Cookie（防 XSS 窃取）
- [ ] 限流存储从内存 `deque` 迁移到 Redis/数据库（支持多实例）
- [ ] 生产环境 CSP 移除 `'unsafe-inline'`（需要构建时 hash/nonce 支持）
- [ ] SQLite `check_same_thread=False` 评估替代方案

**决策理由**：
- 独立开发者工具常被用于存储商业创意和 API Key，安全即产品信任基础
- 分层防御（纵深防御）：任何单点失效都不会导致整体沦陷
- 所有修复均经过 API 自动化测试验证，避免修复引入回归

**影响范围**：`backend/app/auth.py`, `backend/app/main.py`, `backend/app/encryption.py`, `backend/app/routers/*.py`, `frontend/src/services/api.ts`, `frontend/src/App.tsx`, `frontend/src/components/SafeMarkdown.tsx`

---

### DEC-021：AI SSE 真流式转发 + Prompt 精简 + 移除 Retry（2026-04-27 性能优化）

**日期**：2026-04-27

**决策内容**：将后端 `/ai/chat` 从"缓存全部 chunk 后再推送"改为真正的边收边发流式转发；精简阶段原生系统提示词（去除完整 `stage.content.raw` JSON 注入）；移除 AI 回复格式失败的 retry 逻辑（避免二次调用翻倍延迟）；在 `ai_proxy.py` 增加 TTFT（Time To First Token）监控。

**问题根因**：
1. `event_generator()` 先 `await _collect_sse_chunks()` 缓存全部 AI 输出（10-20 秒），完成后才 `yield` 给前端 —— SSE 完全失去流式意义。
2. `enable_stage_loop=true` 时，格式验证失败触发第二次 AI 调用，延迟直接翻倍（20 秒 → 40 秒）。
3. `build_stage_native_system_prompt()` 将完整 `stage.content`（含上百条日历的原始 JSON）通过 `json.dumps(snapshot)` 注入 prompt，导致 prompt 长度达数千 token，DeepSeek 处理极慢。

**修复方案**：
| 文件 | 改动 | 效果 |
|------|------|------|
| `backend/app/routers/ai.py` | `event_generator()` 边收边发，缓存 `[DONE]` 到流结束后再发 meta event | 用户感知延迟从"完整生成时间"降到"首字延迟" |
| `backend/app/routers/ai.py` | 移除 `_collect_sse_chunks` + `validate_stage_native_response` 的 retry 分支 | 消除二次 AI 调用，延迟不再翻倍 |
| `backend/app/services/stage_context.py` | `minimal_snapshot` 替代完整 `snapshot`，仅保留 project_title / current_stage / stage_key / stage_locked / completion | prompt 长度减少 30%-70%，TTFT 显著降低 |
| `backend/app/services/ai_proxy.py` | `chat_completion()` 内记录 `first_chunk_at = time.time() - request_start` | 可量化 TTFT 优化效果 |

**测试验证**：
- `backend/tests/test_ai_chat_perf.py` 包含 3 个自动化测试：
  1. `test_streaming_ttft`：模拟 0.3s 延迟的 AI 响应，验证首 chunk 到达时间 ≈ 0.3s
  2. `test_prompt_length_reduction`：构造 100 条日历的大内容项目，验证 prompt 不再包含 `contentCalendar` 且长度 < 1500
  3. `test_stream_integrity`：验证 SSE 内容提取、sync_payload 结构化、next_question 提取完整性

**决策理由**：
- 用户明确反馈"每个会话访问 AI 的速度都太慢"，这是当前最大体验瓶颈
- SSE 设计目的就是"边生成边推送"，缓存全部后再发送属于架构缺陷
- retry 的代价（延迟翻倍）远大于收益（偶尔格式修正），改用更强的 prompt engineering（"缺一不可"约束）让 AI 一次答对
- `evaluation.evidence` 已包含关键摘要，AI 无需看到完整的原始 JSON 也能给出精准建议

**影响范围**：`backend/app/routers/ai.py`, `backend/app/services/stage_context.py`, `backend/app/services/ai_proxy.py`, `backend/tests/test_ai_chat_perf.py`

**风险提示**：
- meta event 时序从"内容之前"变为"内容之后"，但前端 `onMeta` handler 不依赖时序，只更新 UI 状态，已审查安全
- 移除 retry 后，AI 偶尔格式不对时 `sync_payload` 可能为空，前端已有空值处理逻辑（不显示同步按钮）
- 精简 prompt 后 AI 看不到完整阶段内容，但关键信息（完成度、缺口列表）已保留，足够理解上下文

### DEC-022：Docker 容器化部署（2026-04-28）

**日期**：2026-04-28

**决策内容**：为 SparkBin 提供完整的 Docker 容器化支持，包含开发环境（SQLite）和生产环境（PostgreSQL + Nginx）两套编排配置。

**方案对比**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| 保持手动部署 | 简单，无 Docker 依赖 | 环境不一致，部署门槛高 |
| **Docker Compose（选中）** | 一键启动，环境隔离，生产可复现 | 需要安装 Docker |

**实施细节**：
- `docker/Dockerfile.backend`：Python 3.11 多阶段构建，依赖缓存层
- `docker/Dockerfile.frontend`：Node 构建 + Nginx 静态托管
- `docker-compose.yml`：开发环境，SQLite 卷持久化，可选 Ollama 服务
- `docker-compose.production.yml`：生产环境，PostgreSQL + Nginx 反向代理 + 健康检查
- `docker/nginx.conf`：反向代理到后端 8000，前端静态文件直接由 Nginx 提供

**影响范围**：项目根目录新增 `docker/`, `docker-compose.yml`, `docker-compose.production.yml`, `.env.example`

---

### DEC-023：Ollama 本地 AI 提供商（2026-04-28）

**日期**：2026-04-28

**决策内容**：接入 Ollama 作为第五个 AI 提供商，支持完全离线的本地大模型推理，无需 API Key，保护数据隐私。

**实现要点**：
- 后端 `ai_proxy.py` 将 Ollama 视为 OpenAI 兼容 API（`/v1/chat/completions`）
- 前端 `AIConfigModal.tsx` 对 Ollama 启用 `noApiKey` 模式（隐藏 API Key 输入框）
- 支持任意 Ollama 兼容模型（llama3.2, qwen2.5, mistral 等）
- Docker Compose 中可选添加 `ollama` 服务

**影响范围**：`backend/app/services/ai_proxy.py`, `backend/app/routers/ai.py`, `frontend/src/components/AIConfigModal.tsx`, `frontend/src/services/ai.ts`

---

### DEC-024：许可证从 MIT 迁移至 Elastic License 2.0（2026-04-28）

**日期**：2026-04-28

**决策内容**：将项目许可证从 MIT 改为 Elastic License 2.0（ELv2）。

**决策理由**：
- MIT 协议对云厂商无限制，大型云服务商可直接将 SparkBin 作为托管 SaaS 提供服务，与开源社区形成直接竞争
- Elastic License 2.0 允许个人开发者和小团队自由使用、修改、自托管，但禁止将软件作为托管服务提供给第三方
- 保护项目可持续性，确保社区版和商业托管版有清晰边界

**影响范围**：`LICENSE` 文件、`README.md` 中的 License 章节、所有对外文档中的许可证声明

**用户影响**：
- 自托管用户：无影响，仍可自由使用
- 贡献者：无影响，PR 仍可正常提交
- 云厂商：未经授权不得将 SparkBin 作为托管服务提供

---

### DEC-025：Token Version + Refresh Token Rotation + OAuth State 硬化（2026-04-28 安全审计）

**日期**：2026-04-28

**决策内容**：作为 P0+P1 安全审计的一部分，引入三项核心认证加固机制。

**1. Token Version 机制**：
- `users` 表新增 `token_version` 整数列（默认 1）
- JWT 签发时嵌入 `tv` claim；校验时对比数据库当前版本
- 改密码、重置密码、登出时 `token_version += 1`，使所有旧 Token 全局失效
- 解决「改密码后旧 Token 仍可用」的漏洞

**2. Refresh Token Rotation**：
- 每次调用 `/auth/refresh` 返回全新 token pair（新 access + 新 refresh）
- 前端保存新 pair，旧 refresh token 立即失效
- 解决 refresh token 长期有效、被盗后难以撤销的问题

**3. OAuth State 一次性校验**：
- State 参数使用 JWT 格式（含 `jti`），成功后放入 `_used_oauth_states` 内存集合
- 防止 OAuth State 重放攻击

**影响范围**：`backend/app/auth.py`, `backend/app/routers/auth.py`, `frontend/src/services/api.ts`, `frontend/src/App.tsx`

---

### DEC-026：Next.js 独立宣传网站（landing/）（2026-04-29）

**日期**：2026-04-29

**决策内容**：在主 React 应用之外，新建 `landing/` 目录作为独立的 Next.js 14 宣传网站。

**决策理由**：
- 主应用（Vite + React）是功能密集的单页应用，不适合作为 SEO 友好的宣传页
- Next.js App Router 提供 SSR/SSG、多语言路由、更好的 SEO 和首屏性能
- 宣传站与主应用解耦，可独立部署到 Vercel/Netlify，而主应用部署到自有服务器

**技术细节**：
- Next.js 14 App Router + Tailwind CSS + `next-intl` 多语言
- 支持 `/zh` 和 `/en` 路由，中间件自动重定向
- 包含 Hero、Features、Stages、Pricing、Deployment、CTA、Footer 等区块
- 与主应用共享 Brutalist 设计系统

**影响范围**：新增 `landing/` 目录（约 30 个文件），不影响主应用构建

---

### DEC-027：前端性能全面优化（2026-04-29）

**日期**：2026-04-29

**决策内容**：对前端进行 9 项性能优化，解决首屏加载慢、列表重渲染、AI 聊天卡顿等问题。

**优化项**：
| 优化 | 实现 | 效果 |
|------|------|------|
| 路由懒加载 | `React.lazy()` + `Suspense` 包裹 `ProjectDetail`, `AdminPage` | 首屏 JS 减少约 40% |
| 代码拆分 | `vite.config.ts` 中 `manualChunks` 按页面和 `react-markdown` 分包 | 避免单个大 bundle |
| AIChat 节流 | SSE 消息渲染 200ms `throttle` | 高频流式更新不再卡顿 |
| ProjectCard memo | `React.memo` 包裹 | 项目列表筛选时避免全量重渲染 |
| API 缓存 | `api.ts` 中对 GET 请求做 3 秒内存缓存 | 减少重复请求 |
| 请求取消 | `ProjectDetail` 中 `AbortController` + `isMounted` 标志 | 组件卸载后不再设置状态 |
| 死锁修复 | `ProjectDetail` 中 `cleanup` 时重置 `isLoading` | 快速切换项目不再卡死 |
| Tree-shaking | `MonthView` 改用 `date-fns` 命名导入 | 减少无用代码 |
| 配置稳定 | `RichTextEditor` 中 `useMemo` 稳定 extensions | 避免 TipTap 重复初始化 |

**影响范围**：`frontend/src/App.tsx`, `frontend/vite.config.ts`, `frontend/src/components/AIChat.tsx`, `frontend/src/components/ProjectCard.tsx`, `frontend/src/components/ProjectDetail.tsx`, `frontend/src/services/api.ts`, `frontend/src/components/MonthView.tsx`, `frontend/src/components/RichTextEditor.tsx`

---

## 附录：待决策事项

以下事项尚未做出最终决策，需要时进行讨论：

- [ ] 是否引入 Redis 缓存 AI 调用结果？
- [x] ~~是否从 SQLite 迁移到 PostgreSQL 作为开发默认数据库？~~（保持 SQLite 开发，PostgreSQL 生产）
- [ ] 是否引入 WebSocket 替代 SSE 用于实时协作？
- [x] ~~是否支持 OAuth（GitHub/Google）登录？~~（已实现，见 DEC-016）
- [x] ~~是否引入 Docker 容器化部署？~~（已实现，见 DEC-022）
- [ ] 是否将 JWT 存储从 localStorage 迁移到 httpOnly Cookie？（见 DEC-020 P3 / DEC-025）
- [ ] 是否引入 Redis 替代内存限流？（见 DEC-020 P3）

---

> **文档结束**。每次开新会话时，AI 应快速浏览本文档，确保不会推翻已确定的架构决策。
