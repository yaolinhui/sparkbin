# 2026年4月27日 18:59:49 — 文档对齐与治理规则确立

---

## 一、本次任务目标

1. **文档与代码对齐**：当前代码已包含 OAuth 登录、邮箱注册、功能门控（Free/Pro/Team）、双 Token 认证、AI 宠物持久化等新功能，但对外文档（README、SELF_HOSTING、CONTRIBUTING）仍停留在旧版本，需要全量对齐。
2. **建立文档治理规则**：明确"什么时候写文档、什么时候更新文档、谁来负责"，防止文档再次滞后。
3. **补齐缺失文档**：创建 CHANGELOG.md、GitHub Issue/PR 模板、文档更新规则文档。

---

## 二、需求讨论记录

### 人类开发者的要求

> "我们现在正在不停地更改这个项目，那么针对这个文档，该怎样判定我们需要在什么时候写文档或者更新文档呢？"
> "需要进行对齐。你上面总结的内容，也可以写到我们 doc 文件相对应的位置。"
> "然后你肯定要去写时间，也就是增加一些规则。"

### 确定的方向

- 文档不是一次性任务，是持续过程
- 需要可执行的触发条件（不是"有空再写"）
- 需要把规则固化为文档，放在 docs/ 目录
- 需要创建带时间戳的任务记录，符合项目现有规范

---

## 三、确定的技术方案

### 3.1 文档对齐范围

| 文档 | 当前问题 | 对齐内容 |
|------|---------|---------|
| `README.md` | Features 缺少 OAuth/邮箱/功能门控；Screenshots 是 TODO | 补充实际功能描述；用文字描述六阶段截图 |
| `SELF_HOSTING.md` | 缺少 Alembic 迁移、OAuth 配置、Email 配置、Nginx 配置、生产检查清单 | 全量重写扩充 |
| `CONTRIBUTING.md` | 缺少 E2E 测试说明、依赖 size 检查、migrations 规范 | 补充代码规范细节 |
| `CHANGELOG.md` | 完全缺失 | 基于实际 git log 创建 |
| `.github/ISSUE_TEMPLATE/` | 完全缺失 | 创建 Bug Report + Feature Request 模板 |
| `.github/PULL_REQUEST_TEMPLATE.md` | 完全缺失 | 创建 PR 模板 |
| `docs/02-文档更新规则.md` | 完全缺失 | 创建文档治理规则 |

### 3.2 文档治理规则要点

- **触发条件**：8 类必须更新文档的代码改动场景
- **反例**：5 类不需要更新文档的场景
- **提交规范**：`docs:` 标签强制要求
- **PR 检查**：PR 模板中"文档已更新"勾选项
- **发版冻结**：发版前 5 项文档检查清单
- **债务清单**：当前已知不一致项的跟踪表

---

## 四、执行过程

### 阶段 1：代码状态扫描

- [x] 检查 master 最新提交：`f325a66`（gitignore）→ `ec6e7a5`（OAuth+邮箱）→ `0dec4d5`（功能门控）
- [x] 读取 `.env.example` 确认环境变量实际状态
- [x] 读取 `models.py`、`auth.py`、`App.tsx` 确认实际功能
- [x] 确认 `README.md`、`CONTRIBUTING.md`、`SELF_HOSTING.md` 在 master 上为旧版本

### 阶段 2：文档编写

- [x] 基于最新 master 创建分支 `docs/project-documentation-v2`
- [x] 重写 `README.md`（Features + Screenshots 描述）
- [x] 重写 `SELF_HOSTING.md`（Alembic + OAuth + Email + Nginx + 生产检查清单）
- [x] 更新 `CONTRIBUTING.md`（E2E 测试 + 依赖 size + migrations 规则）
- [x] 创建 `CHANGELOG.md`（基于 git log）
- [x] 创建 `.github/ISSUE_TEMPLATE/bug_report.md`
- [x] 创建 `.github/ISSUE_TEMPLATE/feature_request.md`
- [x] 创建 `.github/PULL_REQUEST_TEMPLATE.md`
- [x] 创建 `docs/02-文档更新规则.md`
- [x] 创建本任务记录文件

---

## 五、验收标准

- [x] README.md 功能列表与实际代码一致（含 OAuth/邮箱验证/功能门控/AI 宠物持久化）
- [x] SELF_HOSTING.md 包含 Alembic 迁移、环境变量对照表、Nginx 配置、生产检查清单
- [x] CONTRIBUTING.md 包含 E2E 测试命令、migrations 禁止修改规则、依赖 size 检查
- [x] CHANGELOG.md 覆盖 v0.1.0 → Unreleased 的全部实际变更
- [x] GitHub Issue/PR 模板已创建
- [x] 文档治理规则已固化为 `docs/02-文档更新规则.md`
- [x] 任务记录已按命名规范归档到 `docs/tasks/`

---

## 六、后续行动

1. **合并文档分支**：将 `docs/project-documentation-v2` 合并到 master
2. **建立惯例**：后续代码 PR 中必须勾选"文档已更新"检查项
3. **债务清零**：下次迭代优先补齐 Screenshots（需实际截图）

---

> **记录完成时间**：2026-04-27 18:59:49
> **任务状态**：已完成
> **分支**：`docs/project-documentation-v2`
