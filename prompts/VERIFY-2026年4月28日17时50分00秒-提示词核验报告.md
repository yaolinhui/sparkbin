# 提示词核验报告

## 核验方法
1. 读取所有 6 份提示词文件
2. 交叉核对代码库中对应文件的实际内容
3. 标记「已确认正确」和「需要修正」的条目

---

## 逐条核验结果

### 任务 #19：修复 admin 密码稳定性 ✅ 已通过
- `backend/app/auth.py` 中 `verify_password` / `init_default_user` 的修改描述与实际代码一致
- `backend/app/main.py` 的 `sys.dont_write_bytecode` 和日志已确认写入
- `backend/start.py` 的 `PYTHONDONTWRITEBYTECODE` 已确认写入
- `backend/tests/test_auth.py` 已创建并通过 13 项测试
- **结论：无需修正**

### 任务 #22：登录状态持久化 ⚠️ 需小幅修正
- `LoginModal.tsx` 描述正确：当前无记住我功能，无用户名持久化
- `api.ts` 描述基本正确：`refreshAccessToken()` 确实缺少失败清理逻辑
- **问题**：`App.tsx` 中的 `startTokenRefreshTimer()` 调用位置描述不够精确（实际已在 `checkAuth` 和 `handleLogin` 中调用，但缺少 token 格式校验前置）
- **结论：已在使用中按需微调，提示词整体可用**

### 任务 #21：登录页面体验与视觉优化 ❌ 需修正
经代码核查，`frontend/src/components/LoginModal.tsx` 中以下功能**已存在**，提示词描述有误：
1. **错误提示**：已有 `AlertCircle` 图标 + `border-brutal-warning` + `bg-brutal-warning/10`（第 224-228 行）
2. **加载状态**：已有 `AUTHENTICATING...` 文案 + `Loader2` 旋转（第 298-302 行）
3. **Tab 切换**：已有 `transition-colors` 和底部高亮边框（第 172-180 行）
4. **输入框聚焦**：已有 `focus:border-brutal-accent focus:outline-none`（第 246-247、266-267 行）

**实际缺失的功能**：
- 全局键盘监听（Enter 提交、Esc 关闭弹窗）
- 用户名输入框 `autoComplete` 应从 `"off"` 改为 `"username"`（密码已是 `"current-password"`）
- 登录按钮在非 loading 时无 active 按下位移（已存在，第 296 行有 `active:translate-x-[2px]`）

**结论：需要重写问题描述和修改清单，避免重复实现已有功能**

### 任务 #20：OAuth 第三方账号绑定与解绑 ⚠️ 需修正
经代码核查，`backend/app/routers/auth.py` 中：
1. **GitHub Connect 端点已存在**：`GET /oauth/github/connect` 和 `GET /oauth/github/connect/callback`（第 856-950 行）
2. 但 **Google 绑定端点不存在**，且现有 connect 仅针对 GitHub 仓库授权场景

提示词说「新增 `POST /auth/oauth/{provider}/bind`」与现有路由风格（`GET /oauth/...`）不一致。

**结论：需要修正端点路径描述，明确说明 GitHub connect 已存在、需扩展为通用绑定能力**

### 任务 #23：个人资料与账户管理独立页面 ✅ 基本正确
- 后端接口已确认存在：`/auth/me`、`GET/PUT /auth/theme`、`GET/PUT /auth/preferred-model`、`PUT /auth/me/pet-config`
- 前端 API 封装已确认存在：`authApi.getMe`、`authApi.setTheme`、`authApi.setPreferredModel`、`authApi.updatePetConfig`、`authApi.logout`
- **结论：无需修正**

### 任务 #24：登录安全性增强 ⚠️ 需修正
经代码核查：
1. `backend/app/schemas.py` 中 `LoginRequest` 只有 `username` 和 `password` 两个字段
2. 若要在登录时传入 `captcha_answer`，**必须修改 `LoginRequest` schema**
3. 当前 `Retry-After` 确实返回固定 300 秒（`auth.py` 第 52 行）
4. 速率限制核心逻辑描述正确

**结论：需要在「允许修改」中补充 `backend/app/schemas.py` 的 `LoginRequest`**

---

## 待修正文件清单
1. `prompts/009-...-登录页面体验与视觉优化.md` — 重写问题描述，删除已有功能
2. `prompts/010-...-OAuth第三方账号绑定与解绑.md` — 修正端点路径，注明 GitHub connect 已存在
3. `prompts/012-...-登录安全性增强.md` — 补充 `schemas.py` 修改项
